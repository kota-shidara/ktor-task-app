# CLAUDE.md

このファイルは、Claude Code (claude.ai/code) がこのプロジェクトで作業する際のガイダンスを提供します。

## 開発コマンド

```bash
./gradlew run         # ローカル起動
./gradlew test        # テスト実行
./gradlew build       # JARビルド
```

## 設定

環境変数でオーバーライド可能: `STORAGE_JDBCURL`, `STORAGE_USER`, `STORAGE_PASSWORD`
