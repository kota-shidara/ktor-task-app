# CLAUDE.md

このファイルは、Claude Code (claude.ai/code) がこのプロジェクトで作業する際のガイダンスを提供します。

## 開発コマンド

```bash
npm run dev           # Vite開発サーバー
npm run build         # 本番ビルド
npm run lint          # ESLint実行
```

## 環境変数

`VITE_API_BASE_URL` - APIのベースURL (デフォルト: `http://localhost:3001`)
